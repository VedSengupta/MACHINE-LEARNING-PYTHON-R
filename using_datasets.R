#using R builtins datasets
?datasets
library(help="datasets")

#To load a datasets
?library
library("datasets")
#to see list of available datasets
data()

#to load datasets
?airmiles
data("airmiles")
airmiles
head(airmiles)

#to see the structure of datasets
str(airmiles)
view(airmiles)

rm(list=ls())

     
     
  